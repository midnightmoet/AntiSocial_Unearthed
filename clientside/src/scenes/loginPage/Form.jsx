

// Formik form that is used for both the login and register pages, will be used to handle the form validation and submission
const Form = () => {
 return (
    <div>Form</div>
 )
};

export default Form;
